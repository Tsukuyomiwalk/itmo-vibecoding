BOT_TOKEN=your_telegram_bot_token_here
# Optional: path to SQLite DB file
DB_PATH=bot.db
# Optional: DeepSeek API key — enables AI features (forward parsing, P&L context, /ask)
# Without this key all AI features degrade silently; bot works as before
DEEPSEEK_API_KEY=your_deepseek_key_here
